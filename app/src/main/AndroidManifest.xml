<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Add other permissions as needed, e.g., for MapView if implemented -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AfricanLiteratureLibraryApp"
        tools:targetApi="31">

        <!-- Activities -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.BookActivity" />
        <activity android:name=".activities.WebViewActivity" />
        <activity android:name=".activities.LayoutGalleryActivity" />

        <!-- Layout Demo Activities -->
        <activity android:name=".activities.layoutviews.LinearLayoutActivity" />
        <activity android:name=".activities.layoutviews.FrameLayoutActivity" />
        <activity android:name=".activities.layoutviews.RelativeLayoutActivity" />
        <activity android:name=".activities.layoutviews.TableLayoutActivity" />
        <activity android:name=".activities.layoutviews.TabLayoutActivity" />

        <!-- Services -->
        <service
            android:name=".services.BookService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".services.BookSyncService"
            android:enabled="true"
            android:exported="false" />

        <!-- Broadcast Receivers -->
        <receiver
            android:name=".receivers.BookReceiver"
            android:exported="false">
            <intent-filter>
                <!-- Custom action for new book added -->
                <action android:name="com.example.africanliteraturelibraryapp.NEW_BOOK_ADDED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".receivers.BookBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <!-- Custom action for sync complete -->
                <action android:name="com.example.africanliteraturelibraryapp.SYNC_COMPLETE" />
                <!-- Example: Listen for system boot completed (requires RECEIVE_BOOT_COMPLETED permission) -->
                <!-- <action android:name="android.intent.action.BOOT_COMPLETED" /> -->
            </intent-filter>
        </receiver>

        <!-- Content Provider -->
        <provider
            android:name=".providers.BookContentProvider"
            android:authorities="com.example.africanliteraturelibraryapp.bookprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

    </application>
</manifest>
